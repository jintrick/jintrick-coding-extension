# Issue v1.28.0: Add Vitest Knowledge Base (RAG)

## 背景と目的
Gemini CLI の `tech-expert` サブエージェントが Vitest に関する正確な情報を提供できるよう、Vitest の最新ドキュメントに基づくナレッジベース（RAG）を統合する。

## 実装内容
- `knowledge/vitest/` 配下に `catalog.json` およびドキュメントキャッシュ (`cache/docs/`) を配置（既存）。
- `knowledge/vitest/detector.json` を作成し、`npm: vitest` による自動検知ルールを定義。
- `knowledge/vitest/SKILL.md` を作成し、Vitest 専門家としての振る舞い（RAG優先、API、Mock、型チェック等）を定義。
- `npm run build` を実行し、`dist/skills/tech-expert-vitest` にパッケージング。

## 検証結果
- `dist/skills/tech-expert-vitest/manifest.json` に `detector.json` の内容が正しく反映されている。
- `dist/skills/tech-expert-vitest/SKILL.md` および `references/` 以下が正しくコピーされている。
- プロジェクト開始時に `vitest` が検知されることを確認（理論上の動作確認）。

## 完了条件
- [x] ブランチ `issue/v1.28.0` での作業。
- [x] `npm run build` の成功。
- [x] `dist/` への成果物の反映。
- [x] Issue ドキュメントの作成。
