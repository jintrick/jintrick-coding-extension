---
id: v2.13.0
type: feat
status: in-progress
created: 2026-04-08
---

# Issue: `coding-rules` スキルの実裁E��吁E��ージェントへの統吁E

## 背景と問題点
現在、コーチE��ングルールの注入はファイル変更直前�EHook (`coding_rules_hook.cjs`) で行われてぁE��。これ�Eコードを記述する段階では機�Eするが、設訁E(`issue-crafter`) めE��ビュー (`code-reviewer`) の段階では遁E��ぎるため、ルールを事前に老E�Eした設計やレビューが行えなぁE��ぁE��問題がある、E

## 解決筁E
- `skills/coding-rules/` に新規スキルを作�Eし、事前にルールを取得するため�Eスクリプト `get-rules.cjs` を実裁E��る、E
- `agents/issue-crafter.md` および `agents/code-reviewer.md` を更新し、設計�Eレビューの前に `coding-rules` スキルを活用してルールを取得�E適用するように持E��を追加する、E

## 実裁E�E容詳細

### 1. `skills/coding-rules/` の作�E
- `SKILL.md` の作�E:
  - エージェントに対し、設計やレビューの前にコーチE��ングルールを取得することを指示する冁E��を記述する、E
- `scripts/get-rules.cjs` の実裁E
  - `.agent/rules/` チE��レクトリ冁E�Eルールファイルをパースし�E力すめENode.js スクリプト、E
  - 褁E��のファイルパスを標準�Eコマンドライン引数として受け付ける（侁E `node scripts/get-rules.cjs src/a.py src/b.js`�E�、ESON斁E���Eのエスケープによるエラーを防ぐため、単純な引数リストとする、E
  - 処琁E��果は、トークン消費の重褁E��防ぐためE**正規化されたJSON形弁E* で標準�E力に返す、E
    ```json
    {
      "rules": {
        "rule1.md": "ルールの本斁E..",
        "rule2.md": "ルールの本斁E.."
      },
      "mapping": {
        "src/a.py": ["rule1.md"],
        "src/b.js": ["rule1.md", "rule2.md"]
      }
    }
    ```
  - 引数が省略された場合�E、すべてのルールめE`"rules"` オブジェクトに格納して出力する、E

### 2. `agents/issue-crafter.md` の更新
- 忁E��スキル�E�もしくは Required Skills�E��Eリストに `coding-rules` を追加する、E
- 設計時に `get-rules.cjs` を実行して対象ファイルに関連するルールを取得し、それをIssueの完亁E��件�E�タスクリスト）に含めるよう持E��を追加する、E

### 3. `agents/code-reviewer.md` の更新
- `tools` リストに `activate_skill` と `run_shell_command` を追加する、E
- レビューを実行する前に `coding-rules` スキルをアクチE��ベ�Eトし、`get-rules.cjs` を実行してプロジェクト�E規紁E��取得し、その規紁E��基づぁE��レビューを行うよう持E��を追加する、E

## ゴール
エージェントがコード�E設計およ�Eレビューを行う前に、�Eロジェクト固有�EコーチE��ングルール (`.agent/rules/`) を�EロアクチE��ブに取得し、コンチE��ストに含められるようになること、E

## チE��チE
- [ ] チE��ト対象: `get-rules.cjs`
- [ ] チE��ト�E容・目皁E `--path` 引数の有無により正しくルールがフィルタリングされるかを確認する、E
- [ ] 検証方法�E手頁E `get-rules.cjs` を引数なし、およ�E特定�E拡張子を含むパスを引数として実行し、�E力されるルールが期征E��りであることを確認する、E
- [ ] チE��ト対象: エージェント�Eロンプト (`issue-crafter`, `code-reviewer`)
- [ ] チE��ト�E容・目皁E 変更された�Eロンプトが正しい持E��を含んでぁE��かを静的に確認する、E
- [ ] 検証方法�E手頁E 対象のMarkdownファイルをレビューし、`activate_skill` めE`get-rules.cjs` の呼び出し指示が適刁E��記述されてぁE��ことを確認する�
