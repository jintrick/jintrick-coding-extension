description = "リモートの最新作業ブランチへ最短で同期・復帰する。"

prompt = """
開発環境の切り替えに伴う同期作業を自動で開始せよ。

### 同期手順
1. `git fetch origin` を実行し、リモートの最新情報を取得せよ。
2. リモートブランチ（`origin/issue/*`）の中から、最終更新が最も新しいブランチを特定せよ。
3. 特定した最新の作業ブランチをチェックアウトせよ.
4. `npm install` を実行し、依存関係を最新の状態に更新せよ。

### 報告
- 同期したブランチ名と、現在の `package.json` のバージョンを報告。
- `/init` コマンドの実行を促し、セッションの知識同期を推奨。
"""
