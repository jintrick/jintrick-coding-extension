commit 5cc708bdf95c84b9689478268d65fcb78386ced7
Merge: 4cc60dc a928923
Author: jintrick <jintrick@gmail.com>
Date:   Sat Mar 7 11:15:23 2026 +0900

    v2.0.0: merge remote changes and release

commit 4cc60dc4d19c9d68de631d266dcd8ac5d21fdf68
Author: jintrick <jintrick@gmail.com>
Date:   Sat Mar 7 11:10:44 2026 +0900

    v2.0.0: release

diff --git a/tools/build.cjs b/tools/build.cjs
index 8c9962e..75f6302 100644
--- a/tools/build.cjs
+++ b/tools/build.cjs
@@ -8,7 +8,8 @@ const hooks = [
   'hooks/scripts/linter_hook.cjs',
   'hooks/scripts/expert_docs_hook.cjs',
   'hooks/scripts/command_fixer_hook.cjs',
-  'hooks/scripts/tech_stack_discovery_hook.cjs'
+  'hooks/scripts/tech_stack_discovery_hook.cjs',
+  'hooks/scripts/toast_notification_hook.cjs'
 ];
 
 const linters = [

commit 3271fa2f1cce37757c196c0a76fe46b31c9733e9
Author: = <=>
Date:   Fri Mar 6 09:54:21 2026 +0900

    v1.30.0: release

diff --git a/tools/build.cjs b/tools/build.cjs
index 8c9962e..7ce5395 100644
--- a/tools/build.cjs
+++ b/tools/build.cjs
@@ -2,7 +2,8 @@ const esbuild = require('esbuild');
 const path = require('path');
 const fs = require('fs');
 
-const CURRENT_DATE = new Date().toISOString().split('T')[0];
+const pkgJson = JSON.parse(fs.readFileSync('package.json', 'utf8'));
+const EXTENSION_VERSION = pkgJson.version;
 
 const hooks = [
   'hooks/scripts/linter_hook.cjs',
@@ -49,6 +50,9 @@ function promoteKnowledge() {
 
   for (const stack of stacks) {
     const targetDir = path.join('skills', `tech-expert-${stack}`);
+    if (fs.existsSync(targetDir)) {
+      fs.rmSync(targetDir, { recursive: true, force: true });
+    }
     fs.mkdirSync(targetDir, { recursive: true });
 
     const stackDir = path.join(knowledgeDir, stack);
@@ -62,12 +66,12 @@ function promoteKnowledge() {
       const detectorPath = path.join(stackDir, 'detector.json');
       try {
         const data = JSON.parse(fs.readFileSync(detectorPath, 'utf8'));
-        
+
         // Convert npm -> file_contains (package.json)
         if (data.npm && typeof data.npm === 'string') {
           detectors.push({ type: "file_contains", file: "package.json", pattern: `"${data.npm}"` });
         }
-        
+
         // Convert files -> file_exists
         if (Array.isArray(data.files)) {
           data.files.forEach(file => {
@@ -76,7 +80,7 @@ function promoteKnowledge() {
             }
           });
         }
-        
+
         // Convert patterns -> specific rules
         if (Array.isArray(data.patterns)) {
           data.patterns.forEach(p => {
@@ -127,7 +131,7 @@ function promoteKnowledge() {
       id: `tech-expert-${stack}`,
       name: `${stack.charAt(0).toUpperCase() + stack.slice(1)} Expert`,
       description: `${stack} に関する技術的な専門知識を提供します。`,
-      version: CURRENT_DATE,
+      version: EXTENSION_VERSION,
       detectors: detectors
     };
     fs.writeFileSync(path.join(targetDir, 'manifest.json'), JSON.stringify(manifest, null, 2));
@@ -139,7 +143,9 @@ function promoteKnowledge() {
       const content = fs.readFileSync(skillMdPath, 'utf8');
       let updated = content;
       if (content.startsWith('---') && !content.includes('version:')) {
-        updated = updated.replace('---', `---\nversion: ${CURRENT_DATE}`);
+        updated = updated.replace('---', `---\nversion: ${EXTENSION_VERSION}`);
+      } else if (content.startsWith('---') && content.includes('version:')) {
+        updated = updated.replace(/version: .*/, `version: ${EXTENSION_VERSION}`);
       }
       if (!content.includes('catalog.json')) {
         updated += `\n\n## ナレッジの活用方法\n${catalogInstruction}\n`;
@@ -149,7 +155,7 @@ function promoteKnowledge() {
       const defaultSkillMd = `---
 name: tech-expert-${stack}
 description: ${stack} に関する技術的な専門知識を提供します。
-version: ${CURRENT_DATE}
+version: ${EXTENSION_VERSION}
 ---
 # ${stack} Expert Skill
 あなたは ${stack} のスペシャリストです。
@@ -178,17 +184,17 @@ ${catalogInstruction}
         copyDir(srcPath, destPath);
       } else {
         fs.copyFileSync(srcPath, destPath);
-        
+
         // Add to catalog if it's a markdown file
         if (entry.name.endsWith('.md')) {
           const content = fs.readFileSync(srcPath, 'utf8');
           const titleMatch = content.match(/^#\s+(.*)/m);
           const title = titleMatch ? titleMatch[1] : entry.name;
-          
+
           // Simple summary: first paragraph after title or first 150 chars
           const body = content.replace(/^#\s+.*$/m, '').trim();
           const summary = body.split('\n')[0].substring(0, 150) || "No summary available.";
-          
+
           catalog.push({
             path: `references/${entry.name}`,
             title: title,
@@ -200,44 +206,54 @@ ${catalogInstruction}
 
     // Write catalog.json
     fs.writeFileSync(path.join(targetRefsDir, 'catalog.json'), JSON.stringify(catalog, null, 2));
-
-    // 2. Generate SKILL.md (Move after catalog generation to ensure instructions are consistent)
-    // ... (rest of the logic)
   }
 }
 
 async function build() {
-  // hooks をビルド
-  for (const hook of hooks) {
-    await esbuild.build({
-      entryPoints: [hook],
-      bundle: true,
-      platform: 'node',
-      outfile: path.join('dist', hook.replace('hooks/scripts/', 'hooks/')),
-      external: ['./linters/*'], // リンターの動的requireは外部参照として残す
-    });
-  }
-
-  // linters をビルド
-  for (const linter of linters) {
-    await esbuild.build({
-      entryPoints: [linter],
-      bundle: true,
-      platform: 'node',
-      outfile: path.join('dist', linter.replace('hooks/scripts/', 'hooks/')),
-    });
+  console.log('[build] Cleaning up dist/hooks...');
+  if (fs.existsSync('dist/hooks')) {
+    fs.rmSync('dist/hooks', { recursive: true, force: true });
   }
 
-  // 古い dist/skills をクリーンアップ
-  if (fs.existsSync('dist/skills')) {
-    fs.rmSync('dist/skills', { recursive: true, force: true });
-  }
+  try {
+    // Wave 1: hooks をビルド
+    const hookTasks = [];
+    for (const hook of hooks) {
+      console.log(`[build:esbuild] Starting: ${hook}`);
+      const p = esbuild.build({
+        entryPoints: [hook],
+        bundle: true,
+        platform: 'node',
+        outfile: path.join('dist', hook.replace('hooks/scripts/', 'hooks/')),
+        external: ['./linters/*'], // リンターの動的requireは外部参照として残す
+      }).then(() => console.log(`[build:esbuild] Finished: ${hook}`));
+      hookTasks.push(p);
+    }
+    await Promise.all(hookTasks);
+
+    // Wave 2: linters をビルド
+    const linterTasks = [];
+    for (const linter of linters) {
+      console.log(`[build:esbuild] Starting: ${linter}`);
+      const p = esbuild.build({
+        entryPoints: [linter],
+        bundle: true,
+        platform: 'node',
+        outfile: path.join('dist', linter.replace('hooks/scripts/', 'hooks/')),
+      }).then(() => console.log(`[build:esbuild] Finished: ${linter}`));
+      linterTasks.push(p);
+    }
+    await Promise.all(linterTasks);
 
-  // knowledge を昇格
-  console.log('Promoting knowledge to skills...');
-  promoteKnowledge();
+    // Wave 3: knowledge を昇格
+    console.log('Promoting knowledge to skills...');
+    promoteKnowledge();
 
-  console.log('Build completed successfully!');
+    console.log('Build completed successfully!');
+  } catch (err) {
+    console.error('[build] One or more tasks failed:', err);
+    process.exit(1);
+  }
 }
 
 build().catch(err => {

commit ccad98e100b37e55a7982d388f921b4c5304b97b
Author: = <=>
Date:   Tue Mar 3 19:02:59 2026 +0900

    fix: standardize skill discovery and deprecate dist/skills (v1.29.0)

diff --git a/tools/build.cjs b/tools/build.cjs
index fed9510..8c9962e 100644
--- a/tools/build.cjs
+++ b/tools/build.cjs
@@ -48,7 +48,7 @@ function promoteKnowledge() {
     .map(dirent => dirent.name);
 
   for (const stack of stacks) {
-    const targetDir = path.join('dist/skills', `tech-expert-${stack}`);
+    const targetDir = path.join('skills', `tech-expert-${stack}`);
     fs.mkdirSync(targetDir, { recursive: true });
 
     const stackDir = path.join(knowledgeDir, stack);
@@ -228,9 +228,10 @@ async function build() {
     });
   }
 
-  // skills ディレクトリをコピー
-  console.log('Copying skills...');
-  copyDir('skills', 'dist/skills');
+  // 古い dist/skills をクリーンアップ
+  if (fs.existsSync('dist/skills')) {
+    fs.rmSync('dist/skills', { recursive: true, force: true });
+  }
 
   // knowledge を昇格
   console.log('Promoting knowledge to skills...');

commit 2cc87b1595e699ae8bd75939cbd271e1e3002c12
Author: = <=>
Date:   Tue Mar 3 11:34:57 2026 +0900

    v1.27.1: release

diff --git a/tools/build.cjs b/tools/build.cjs
index 32f9c20..fed9510 100644
--- a/tools/build.cjs
+++ b/tools/build.cjs
@@ -117,9 +117,9 @@ function promoteKnowledge() {
         }
       }
 
-      // Final fallback
+      // Final check: throw error if no detectors could be generated
       if (detectors.length === 0) {
-        detectors.push({ type: "file_exists", file: `${stack}.config.js` });
+        throw new Error(`[build] No detector rules could be generated for knowledge base '${stack}'. Please provide a valid 'detector.json' file.`);
       }
     }
 

commit c012ccbc46b0d7839f83958fa830f7a839ea6b9a
Author: = <=>
Date:   Tue Mar 3 10:52:55 2026 +0900

    v1.27.0: release

diff --git a/tools/build.cjs b/tools/build.cjs
index 0cc697b..32f9c20 100644
--- a/tools/build.cjs
+++ b/tools/build.cjs
@@ -55,40 +55,72 @@ function promoteKnowledge() {
 
     // 1. Generate manifest.json
     let detectors = [];
-
-    // Scan all files in the stack knowledge directory
     const stackFiles = fs.readdirSync(stackDir);
 
-    // Common detector inference rules
-    if (stackFiles.includes('package.json')) {
-      const packageJsonPath = path.join(stackDir, 'package.json');
+    // 1.1 Priority: detector.json (Issue v1.27.0 implementation)
+    if (stackFiles.includes('detector.json')) {
+      const detectorPath = path.join(stackDir, 'detector.json');
       try {
-        const pkg = JSON.parse(fs.readFileSync(packageJsonPath, 'utf8'));
-        const name = pkg.name || stack;
-        detectors.push({ type: "file_contains", file: "package.json", pattern: `"${name}"` });
+        const data = JSON.parse(fs.readFileSync(detectorPath, 'utf8'));
+        
+        // Convert npm -> file_contains (package.json)
+        if (data.npm && typeof data.npm === 'string') {
+          detectors.push({ type: "file_contains", file: "package.json", pattern: `"${data.npm}"` });
+        }
+        
+        // Convert files -> file_exists
+        if (Array.isArray(data.files)) {
+          data.files.forEach(file => {
+            if (typeof file === 'string') {
+              detectors.push({ type: "file_exists", file: file });
+            }
+          });
+        }
+        
+        // Convert patterns -> specific rules
+        if (Array.isArray(data.patterns)) {
+          data.patterns.forEach(p => {
+            if (p.file && p.pattern) {
+              detectors.push({ type: "file_contains", file: p.file, pattern: p.pattern });
+            }
+          });
+        }
       } catch (e) {
-        console.warn(`[build] Failed to parse ${packageJsonPath}, falling back to directory name. Error: ${e.message}`);
-        detectors.push({ type: "file_contains", file: "package.json", pattern: `"${stack}"` });
+        console.warn(`[build] Failed to parse ${detectorPath}, falling back to inference. Error: ${e.message}`);
       }
     }
 
-    if (stackFiles.includes('requirements.txt')) {
-      detectors.push({ type: "file_exists", file: "requirements.txt" });
-    }
-    if (stackFiles.includes('pyproject.toml')) {
-      detectors.push({ type: "file_exists", file: "pyproject.toml" });
-    }
+    // 1.2 Fallback: Inference rules (only if no valid detectors from detector.json)
+    if (detectors.length === 0) {
+      if (stackFiles.includes('package.json')) {
+        const packageJsonPath = path.join(stackDir, 'package.json');
+        try {
+          const pkg = JSON.parse(fs.readFileSync(packageJsonPath, 'utf8'));
+          const name = pkg.name || stack;
+          detectors.push({ type: "file_contains", file: "package.json", pattern: `"${name}"` });
+        } catch (e) {
+          console.warn(`[build] Failed to parse ${packageJsonPath}, falling back to directory name. Error: ${e.message}`);
+          detectors.push({ type: "file_contains", file: "package.json", pattern: `"${stack}"` });
+        }
+      }
 
-    // Config file inference: any file ending with .config.js or .config.ts or vite.config.*, next.config.*
-    for (const file of stackFiles) {
-      if (file.endsWith('.config.js') || file.endsWith('.config.ts') || file.startsWith('vite.config.') || file.startsWith('next.config.')) {
-        detectors.push({ type: "file_exists", file: file });
+      if (stackFiles.includes('requirements.txt')) {
+        detectors.push({ type: "file_exists", file: "requirements.txt" });
+      }
+      if (stackFiles.includes('pyproject.toml')) {
+        detectors.push({ type: "file_exists", file: "pyproject.toml" });
       }
-    }
 
-    // Fallback if no detectors found
-    if (detectors.length === 0) {
-      detectors.push({ type: "file_exists", file: `${stack}.config.js` });
+      for (const file of stackFiles) {
+        if (file.endsWith('.config.js') || file.endsWith('.config.ts') || file.startsWith('vite.config.') || file.startsWith('next.config.')) {
+          detectors.push({ type: "file_exists", file: file });
+        }
+      }
+
+      // Final fallback
+      if (detectors.length === 0) {
+        detectors.push({ type: "file_exists", file: `${stack}.config.js` });
+      }
     }
 
     const manifest = {

commit f74123340441a073d9dce0b55b3daa76155c20cf
Author: = <=>
Date:   Tue Mar 3 10:13:32 2026 +0900

    v1.26.2: release

diff --git a/tools/build.cjs b/tools/build.cjs
index a5f75ed..0cc697b 100644
--- a/tools/build.cjs
+++ b/tools/build.cjs
@@ -101,15 +101,18 @@ function promoteKnowledge() {
     fs.writeFileSync(path.join(targetDir, 'manifest.json'), JSON.stringify(manifest, null, 2));
 
     // 2. Generate SKILL.md
+    const catalogInstruction = `回答の際は、まず \`references/catalog.json\` を読み込み、質問に関連するドキュメントを特定した上で、そのドキュメントの内容に基づいて回答してください。`;
     const skillMdPath = path.join(stackDir, 'SKILL.md');
     if (fs.existsSync(skillMdPath)) {
       const content = fs.readFileSync(skillMdPath, 'utf8');
+      let updated = content;
       if (content.startsWith('---') && !content.includes('version:')) {
-        const updated = content.replace('---', `---\nversion: ${CURRENT_DATE}`);
-        fs.writeFileSync(path.join(targetDir, 'SKILL.md'), updated);
-      } else {
-        fs.copyFileSync(skillMdPath, path.join(targetDir, 'SKILL.md'));
+        updated = updated.replace('---', `---\nversion: ${CURRENT_DATE}`);
+      }
+      if (!content.includes('catalog.json')) {
+        updated += `\n\n## ナレッジの活用方法\n${catalogInstruction}\n`;
       }
+      fs.writeFileSync(path.join(targetDir, 'SKILL.md'), updated);
     } else {
       const defaultSkillMd = `---
 name: tech-expert-${stack}
@@ -117,7 +120,10 @@ description: ${stack} に関する技術的な専門知識を提供します。
 version: ${CURRENT_DATE}
 ---
 # ${stack} Expert Skill
-あなたは ${stack} のスペシャリストです。\`references/\` 内のドキュメントに基づき、専門的な助言を行います。
+あなたは ${stack} のスペシャリストです。
+
+## 動作指示
+${catalogInstruction}
 `;
       fs.writeFileSync(path.join(targetDir, 'SKILL.md'), defaultSkillMd);
     }
@@ -128,6 +134,7 @@ version: ${CURRENT_DATE}
 
     const entries = fs.readdirSync(stackDir, { withFileTypes: true });
     const ignoreFiles = ['package.json', 'requirements.txt', 'pyproject.toml', 'SKILL.md'];
+    const catalog = [];
 
     for (const entry of entries) {
       if (ignoreFiles.includes(entry.name)) continue;
@@ -139,8 +146,31 @@ version: ${CURRENT_DATE}
         copyDir(srcPath, destPath);
       } else {
         fs.copyFileSync(srcPath, destPath);
+        
+        // Add to catalog if it's a markdown file
+        if (entry.name.endsWith('.md')) {
+          const content = fs.readFileSync(srcPath, 'utf8');
+          const titleMatch = content.match(/^#\s+(.*)/m);
+          const title = titleMatch ? titleMatch[1] : entry.name;
+          
+          // Simple summary: first paragraph after title or first 150 chars
+          const body = content.replace(/^#\s+.*$/m, '').trim();
+          const summary = body.split('\n')[0].substring(0, 150) || "No summary available.";
+          
+          catalog.push({
+            path: `references/${entry.name}`,
+            title: title,
+            summary: summary
+          });
+        }
       }
     }
+
+    // Write catalog.json
+    fs.writeFileSync(path.join(targetRefsDir, 'catalog.json'), JSON.stringify(catalog, null, 2));
+
+    // 2. Generate SKILL.md (Move after catalog generation to ensure instructions are consistent)
+    // ... (rest of the logic)
   }
 }
 

commit cb9e6f64e3cdcd728da5445c34238f6765b8764b
Author: = <=>
Date:   Tue Mar 3 08:03:58 2026 +0900

    v1.26.1: release

diff --git a/tools/build.cjs b/tools/build.cjs
index 4f40505..a5f75ed 100644
--- a/tools/build.cjs
+++ b/tools/build.cjs
@@ -2,6 +2,8 @@ const esbuild = require('esbuild');
 const path = require('path');
 const fs = require('fs');
 
+const CURRENT_DATE = new Date().toISOString().split('T')[0];
+
 const hooks = [
   'hooks/scripts/linter_hook.cjs',
   'hooks/scripts/expert_docs_hook.cjs',
@@ -93,7 +95,7 @@ function promoteKnowledge() {
       id: `tech-expert-${stack}`,
       name: `${stack.charAt(0).toUpperCase() + stack.slice(1)} Expert`,
       description: `${stack} に関する技術的な専門知識を提供します。`,
-      version: "1.0.0",
+      version: CURRENT_DATE,
       detectors: detectors
     };
     fs.writeFileSync(path.join(targetDir, 'manifest.json'), JSON.stringify(manifest, null, 2));
@@ -103,7 +105,7 @@ function promoteKnowledge() {
     if (fs.existsSync(skillMdPath)) {
       const content = fs.readFileSync(skillMdPath, 'utf8');
       if (content.startsWith('---') && !content.includes('version:')) {
-        const updated = content.replace('---', `---\nversion: 1.0.0`);
+        const updated = content.replace('---', `---\nversion: ${CURRENT_DATE}`);
         fs.writeFileSync(path.join(targetDir, 'SKILL.md'), updated);
       } else {
         fs.copyFileSync(skillMdPath, path.join(targetDir, 'SKILL.md'));
@@ -112,7 +114,7 @@ function promoteKnowledge() {
       const defaultSkillMd = `---
 name: tech-expert-${stack}
 description: ${stack} に関する技術的な専門知識を提供します。
-version: 1.0.0
+version: ${CURRENT_DATE}
 ---
 # ${stack} Expert Skill
 あなたは ${stack} のスペシャリストです。\`references/\` 内のドキュメントに基づき、専門的な助言を行います。

commit a343dbd79047b1010224c79db68665cdecdd1d00
Author: = <=>
Date:   Mon Mar 2 14:53:27 2026 +0900

    fix: minor adjustments by gemini (robust config parsing and log)

diff --git a/tools/build.cjs b/tools/build.cjs
index 5f57817..4f40505 100644
--- a/tools/build.cjs
+++ b/tools/build.cjs
@@ -65,6 +65,7 @@ function promoteKnowledge() {
         const name = pkg.name || stack;
         detectors.push({ type: "file_contains", file: "package.json", pattern: `"${name}"` });
       } catch (e) {
+        console.warn(`[build] Failed to parse ${packageJsonPath}, falling back to directory name. Error: ${e.message}`);
         detectors.push({ type: "file_contains", file: "package.json", pattern: `"${stack}"` });
       }
     }
@@ -78,7 +79,7 @@ function promoteKnowledge() {
 
     // Config file inference: any file ending with .config.js or .config.ts or vite.config.*, next.config.*
     for (const file of stackFiles) {
-      if (file.endsWith('.config.js') || file.endsWith('.config.ts')) {
+      if (file.endsWith('.config.js') || file.endsWith('.config.ts') || file.startsWith('vite.config.') || file.startsWith('next.config.')) {
         detectors.push({ type: "file_exists", file: file });
       }
     }

commit 99b02d7fc8b4fd31a1415012e2d8ece88938409a
Author: = <=>
Date:   Mon Mar 2 14:46:59 2026 +0900

    fix: minor adjustments by gemini (add version fields to skills)

diff --git a/tools/build.cjs b/tools/build.cjs
index 8e93e32..5f57817 100644
--- a/tools/build.cjs
+++ b/tools/build.cjs
@@ -92,6 +92,7 @@ function promoteKnowledge() {
       id: `tech-expert-${stack}`,
       name: `${stack.charAt(0).toUpperCase() + stack.slice(1)} Expert`,
       description: `${stack} に関する技術的な専門知識を提供します。`,
+      version: "1.0.0",
       detectors: detectors
     };
     fs.writeFileSync(path.join(targetDir, 'manifest.json'), JSON.stringify(manifest, null, 2));
@@ -99,11 +100,18 @@ function promoteKnowledge() {
     // 2. Generate SKILL.md
     const skillMdPath = path.join(stackDir, 'SKILL.md');
     if (fs.existsSync(skillMdPath)) {
-      fs.copyFileSync(skillMdPath, path.join(targetDir, 'SKILL.md'));
+      const content = fs.readFileSync(skillMdPath, 'utf8');
+      if (content.startsWith('---') && !content.includes('version:')) {
+        const updated = content.replace('---', `---\nversion: 1.0.0`);
+        fs.writeFileSync(path.join(targetDir, 'SKILL.md'), updated);
+      } else {
+        fs.copyFileSync(skillMdPath, path.join(targetDir, 'SKILL.md'));
+      }
     } else {
       const defaultSkillMd = `---
 name: tech-expert-${stack}
 description: ${stack} に関する技術的な専門知識を提供します。
+version: 1.0.0
 ---
 # ${stack} Expert Skill
 あなたは ${stack} のスペシャリストです。\`references/\` 内のドキュメントに基づき、専門的な助言を行います。

commit 2ee463e4710a37ebf71fe3bdbe64745bc4380919
Author: google-labs-jules[bot] <161369871+google-labs-jules[bot]@users.noreply.github.com>
Date:   Mon Mar 2 05:44:51 2026 +0000

    feat: implement zero-config skill promotion for tech-expert
    
    - Added `knowledge/` directory as the new source of truth for tech-expert skills.
    - Implemented `promoteKnowledge()` in `tools/build.cjs` to automatically generate `dist/skills/tech-expert-*` and infer `manifest.json` rules based on the contents of `knowledge/<stack>/`.
    - Updated `tech_stack_discovery_hook.cjs` to search for manifestations primarily in `dist/skills/`.
    - Updated documentation in `docs/reference/tech-expert-spec.md` to deprecate manual skill creation and describe the new zero-config architecture.
    - Removed old manually created `skills/tech-expert-*` directories.
    - Ensured tests pass and updated tests to reflect `tech_stack_discovery_hook`'s updated behavior.
    
    Co-authored-by: jintrick <1336563+jintrick@users.noreply.github.com>

diff --git a/tools/build.cjs b/tools/build.cjs
index 2889394..8e93e32 100644
--- a/tools/build.cjs
+++ b/tools/build.cjs
@@ -37,6 +37,102 @@ function copyDir(src, dest) {
   }
 }
 
+function promoteKnowledge() {
+  const knowledgeDir = 'knowledge';
+  if (!fs.existsSync(knowledgeDir)) return;
+
+  const stacks = fs.readdirSync(knowledgeDir, { withFileTypes: true })
+    .filter(dirent => dirent.isDirectory())
+    .map(dirent => dirent.name);
+
+  for (const stack of stacks) {
+    const targetDir = path.join('dist/skills', `tech-expert-${stack}`);
+    fs.mkdirSync(targetDir, { recursive: true });
+
+    const stackDir = path.join(knowledgeDir, stack);
+
+    // 1. Generate manifest.json
+    let detectors = [];
+
+    // Scan all files in the stack knowledge directory
+    const stackFiles = fs.readdirSync(stackDir);
+
+    // Common detector inference rules
+    if (stackFiles.includes('package.json')) {
+      const packageJsonPath = path.join(stackDir, 'package.json');
+      try {
+        const pkg = JSON.parse(fs.readFileSync(packageJsonPath, 'utf8'));
+        const name = pkg.name || stack;
+        detectors.push({ type: "file_contains", file: "package.json", pattern: `"${name}"` });
+      } catch (e) {
+        detectors.push({ type: "file_contains", file: "package.json", pattern: `"${stack}"` });
+      }
+    }
+
+    if (stackFiles.includes('requirements.txt')) {
+      detectors.push({ type: "file_exists", file: "requirements.txt" });
+    }
+    if (stackFiles.includes('pyproject.toml')) {
+      detectors.push({ type: "file_exists", file: "pyproject.toml" });
+    }
+
+    // Config file inference: any file ending with .config.js or .config.ts or vite.config.*, next.config.*
+    for (const file of stackFiles) {
+      if (file.endsWith('.config.js') || file.endsWith('.config.ts')) {
+        detectors.push({ type: "file_exists", file: file });
+      }
+    }
+
+    // Fallback if no detectors found
+    if (detectors.length === 0) {
+      detectors.push({ type: "file_exists", file: `${stack}.config.js` });
+    }
+
+    const manifest = {
+      id: `tech-expert-${stack}`,
+      name: `${stack.charAt(0).toUpperCase() + stack.slice(1)} Expert`,
+      description: `${stack} に関する技術的な専門知識を提供します。`,
+      detectors: detectors
+    };
+    fs.writeFileSync(path.join(targetDir, 'manifest.json'), JSON.stringify(manifest, null, 2));
+
+    // 2. Generate SKILL.md
+    const skillMdPath = path.join(stackDir, 'SKILL.md');
+    if (fs.existsSync(skillMdPath)) {
+      fs.copyFileSync(skillMdPath, path.join(targetDir, 'SKILL.md'));
+    } else {
+      const defaultSkillMd = `---
+name: tech-expert-${stack}
+description: ${stack} に関する技術的な専門知識を提供します。
+---
+# ${stack} Expert Skill
+あなたは ${stack} のスペシャリストです。\`references/\` 内のドキュメントに基づき、専門的な助言を行います。
+`;
+      fs.writeFileSync(path.join(targetDir, 'SKILL.md'), defaultSkillMd);
+    }
+
+    // 3. Copy contents to references/
+    const targetRefsDir = path.join(targetDir, 'references');
+    fs.mkdirSync(targetRefsDir, { recursive: true });
+
+    const entries = fs.readdirSync(stackDir, { withFileTypes: true });
+    const ignoreFiles = ['package.json', 'requirements.txt', 'pyproject.toml', 'SKILL.md'];
+
+    for (const entry of entries) {
+      if (ignoreFiles.includes(entry.name)) continue;
+
+      const srcPath = path.join(stackDir, entry.name);
+      const destPath = path.join(targetRefsDir, entry.name);
+
+      if (entry.isDirectory()) {
+        copyDir(srcPath, destPath);
+      } else {
+        fs.copyFileSync(srcPath, destPath);
+      }
+    }
+  }
+}
+
 async function build() {
   // hooks をビルド
   for (const hook of hooks) {
@@ -63,6 +159,10 @@ async function build() {
   console.log('Copying skills...');
   copyDir('skills', 'dist/skills');
 
+  // knowledge を昇格
+  console.log('Promoting knowledge to skills...');
+  promoteKnowledge();
+
   console.log('Build completed successfully!');
 }
 

commit af1c3ddcc2186d6c902ce593632d48187bce247f
Author: jintrick <jintrick@gmail.com>
Date:   Sun Mar 1 14:58:11 2026 +0900

    feat: implement unified tech-expert agent with plug-and-play RAG

diff --git a/tools/build.cjs b/tools/build.cjs
index 9878210..2889394 100644
--- a/tools/build.cjs
+++ b/tools/build.cjs
@@ -5,7 +5,8 @@ const fs = require('fs');
 const hooks = [
   'hooks/scripts/linter_hook.cjs',
   'hooks/scripts/expert_docs_hook.cjs',
-  'hooks/scripts/command_fixer_hook.cjs'
+  'hooks/scripts/command_fixer_hook.cjs',
+  'hooks/scripts/tech_stack_discovery_hook.cjs'
 ];
 
 const linters = [

commit a24a3e63b7b5db4116f16309b08bc732e40cc667
Author: google-labs-jules[bot] <161369871+google-labs-jules[bot]@users.noreply.github.com>
Date:   Thu Feb 26 07:00:22 2026 +0000

    I have implemented the MS Access database skill. Here is a summary of the changes:
    
    - Added `skills/access-db` containing `SKILL.md` and `db_client.cjs`.
    - The `db_client.cjs` script uses `node-adodb` to execute SQL on `.accdb` files.
    - Added unit tests in `tests/skills/access-db.test.js` that mock the external dependency.
    - Updated `tools/build.cjs` to copy the `skills/` directory to the distribution folder.
    - Marked the status in `docs/issue/v1.20.0.md` as complete.
    - Increased the package version to 1.20.0.
    - Updated `.gitignore` to include `dist/skills/`.
    
    Co-authored-by: jintrick <1336563+jintrick@users.noreply.github.com>

diff --git a/tools/build.cjs b/tools/build.cjs
index 1936497..9878210 100644
--- a/tools/build.cjs
+++ b/tools/build.cjs
@@ -19,6 +19,23 @@ const linters = [
   'hooks/scripts/linters/py.cjs'
 ];
 
+function copyDir(src, dest) {
+  if (!fs.existsSync(src)) return;
+  fs.mkdirSync(dest, { recursive: true });
+  let entries = fs.readdirSync(src, { withFileTypes: true });
+
+  for (let entry of entries) {
+    let srcPath = path.join(src, entry.name);
+    let destPath = path.join(dest, entry.name);
+
+    if (entry.isDirectory()) {
+      copyDir(srcPath, destPath);
+    } else {
+      fs.copyFileSync(srcPath, destPath);
+    }
+  }
+}
+
 async function build() {
   // hooks をビルド
   for (const hook of hooks) {
@@ -41,6 +58,10 @@ async function build() {
     });
   }
 
+  // skills ディレクトリをコピー
+  console.log('Copying skills...');
+  copyDir('skills', 'dist/skills');
+
   console.log('Build completed successfully!');
 }
 

commit 715bc904445c31e6fd13e21c98abb6ed6094b5ea
Author: google-labs-jules[bot] <161369871+google-labs-jules[bot]@users.noreply.github.com>
Date:   Wed Feb 25 03:05:24 2026 +0000

    I have introduced the `release-manager` skill and removed the `idd_sync_hook`. This deprecates the automatic sync hook in favor of an explicit version completion command and the `release-manager` skill. This ensures a deterministic and explicit release preparation process, respecting `RELEASE.md`.
    
    Changes:
    - Add configuration for the new completion command.
    - Add `skills/release-manager/SKILL.md`.
    - Add `skills/release-manager/scripts/update_version.cjs`.
    - Add `skills/release-manager/tests/update_version.test.js`.
    - Remove `hooks/hooks.json` entry for `idd-sync-hook`.
    - Remove `hooks/scripts/idd_sync_hook.cjs` and `hooks/scripts/idd_sync/`.
    - Remove `tests/hooks/idd_sync_hook.test.js`.
    - Update `tools/build.cjs` to remove `idd_sync_hook.cjs` from the build process.
    - Update `docs/reference/idd-flow.md` to reflect the new release workflow.
    
    Co-authored-by: jintrick <1336563+jintrick@users.noreply.github.com>

diff --git a/tools/build.cjs b/tools/build.cjs
index d95e5ef..1936497 100644
--- a/tools/build.cjs
+++ b/tools/build.cjs
@@ -5,8 +5,7 @@ const fs = require('fs');
 const hooks = [
   'hooks/scripts/linter_hook.cjs',
   'hooks/scripts/expert_docs_hook.cjs',
-  'hooks/scripts/command_fixer_hook.cjs',
-  'hooks/scripts/idd_sync_hook.cjs'
+  'hooks/scripts/command_fixer_hook.cjs'
 ];
 
 const linters = [

commit 579538abd7f36ba5306d3ee9b41a44b978f92da4
Author: google-labs-jules[bot] <161369871+google-labs-jules[bot]@users.noreply.github.com>
Date:   Sat Feb 21 11:45:41 2026 +0000

    feat: Add Python and TSX support to linter_hook
    
    - Added `hooks/scripts/linters/py.cjs` for Python syntax checking using `python -c`.
    - Modified `hooks/scripts/linters/ts.cjs` to support `.tsx` files.
    - Added `hooks/scripts/linters/tsx.cjs` as a bridge to `ts.cjs`.
    - Updated `hooks/scripts/linter_hook.cjs` to support `.py` and `.tsx` extensions.
    - Updated `tools/build.cjs` to include new linters.
    - Added tests in `tests/hooks/linter_hook.test.js`.
    
    Co-authored-by: jintrick <1336563+jintrick@users.noreply.github.com>

diff --git a/tools/build.cjs b/tools/build.cjs
index 740bb1c..d95e5ef 100644
--- a/tools/build.cjs
+++ b/tools/build.cjs
@@ -14,8 +14,10 @@ const linters = [
   'hooks/scripts/linters/cjs.cjs',
   'hooks/scripts/linters/mjs.cjs',
   'hooks/scripts/linters/ts.cjs',
+  'hooks/scripts/linters/tsx.cjs',
   'hooks/scripts/linters/json.cjs',
-  'hooks/scripts/linters/md.cjs'
+  'hooks/scripts/linters/md.cjs',
+  'hooks/scripts/linters/py.cjs'
 ];
 
 async function build() {

commit 80fa0c85b0b1ff4557e4b9abea677207ed2c46ec
Author: jintrick <jintrick@gmail.com>
Date:   Sat Feb 21 18:53:56 2026 +0900

    Implement IDD Sync Hook (#5)
    
    * feat: implement IDD Sync Hook for automatic version synchronization
    
    - Added `smol-toml` dependency for TOML parsing.
    - Implemented `hooks/scripts/idd_sync_hook.cjs` to intercept `git commit` and synchronize versions.
    - Added support for updating `package.json`, `gemini-extension.json`, `pyproject.toml`, and `Cargo.toml`.
    - Implemented logic to rewrite `git commit` command to include `git add` for updated files.
    - Added unit tests in `tests/hooks/idd_sync_hook.test.js`.
    - Registered the new hook in `hooks/hooks.json`.
    - Updated `tools/build.cjs` to include the new hook in the build process.
    
    Co-authored-by: jintrick <1336563+jintrick@users.noreply.github.com>
    
    * feat: implement IDD Sync Hook for automatic version synchronization
    
    - Added `smol-toml` dependency for TOML parsing.
    - Implemented `hooks/scripts/idd_sync_hook.cjs` to intercept `git commit` and synchronize versions.
    - Added support for updating `package.json`, `gemini-extension.json`, `pyproject.toml`, and `Cargo.toml`.
    - Implemented logic to rewrite `git commit` command to include `git add` for updated files.
    - Added support for pre-release versions (e.g., `v1.15.0-rc.1`).
    - Added unit tests in `tests/hooks/idd_sync_hook.test.js`.
    - Registered the new hook in `hooks/hooks.json`.
    - Updated `tools/build.cjs` to include the new hook in the build process.
    - Bumped project version to 1.15.0.
    
    Co-authored-by: jintrick <1336563+jintrick@users.noreply.github.com>
    
    * fix: minor adjustments by gemini (command separator, error logging, and dependencies)
    
    ---------
    
    Co-authored-by: google-labs-jules[bot] <161369871+google-labs-jules[bot]@users.noreply.github.com>
    Co-authored-by: jintrick <1336563+jintrick@users.noreply.github.com>

diff --git a/tools/build.cjs b/tools/build.cjs
index fccd0d4..740bb1c 100644
--- a/tools/build.cjs
+++ b/tools/build.cjs
@@ -5,7 +5,8 @@ const fs = require('fs');
 const hooks = [
   'hooks/scripts/linter_hook.cjs',
   'hooks/scripts/expert_docs_hook.cjs',
-  'hooks/scripts/command_fixer_hook.cjs'
+  'hooks/scripts/command_fixer_hook.cjs',
+  'hooks/scripts/idd_sync_hook.cjs'
 ];
 
 const linters = [

commit f5a3f9fe8e47e1e51e15b83c6b6266ae0e864d29
Author: jintrick <jintrick@gmail.com>
Date:   Sat Feb 21 07:32:57 2026 +0900

    feat(issue_v1.13.0): implement Command Fixer Hook for Windows compatibility

diff --git a/tools/build.cjs b/tools/build.cjs
index 7c72dd1..fccd0d4 100644
--- a/tools/build.cjs
+++ b/tools/build.cjs
@@ -4,7 +4,8 @@ const fs = require('fs');
 
 const hooks = [
   'hooks/scripts/linter_hook.cjs',
-  'hooks/scripts/expert_docs_hook.cjs'
+  'hooks/scripts/expert_docs_hook.cjs',
+  'hooks/scripts/command_fixer_hook.cjs'
 ];
 
 const linters = [

commit 7d6c260097aa5a443921e02d48a38ce217ab7d90
Author: jintrick <jintrick@gmail.com>
Date:   Wed Feb 18 06:08:32 2026 +0900

    fix(hooks): restore temp-file preview approach in BeforeTool, remove blocking and buggy AfterTool

diff --git a/tools/build.cjs b/tools/build.cjs
index 44a3a83..7c72dd1 100644
--- a/tools/build.cjs
+++ b/tools/build.cjs
@@ -4,8 +4,7 @@ const fs = require('fs');
 
 const hooks = [
   'hooks/scripts/linter_hook.cjs',
-  'hooks/scripts/expert_docs_hook.cjs',
-  'hooks/scripts/open_file_hook.cjs'
+  'hooks/scripts/expert_docs_hook.cjs'
 ];
 
 const linters = [
@@ -13,7 +12,8 @@ const linters = [
   'hooks/scripts/linters/cjs.cjs',
   'hooks/scripts/linters/mjs.cjs',
   'hooks/scripts/linters/ts.cjs',
-  'hooks/scripts/linters/json.cjs'
+  'hooks/scripts/linters/json.cjs',
+  'hooks/scripts/linters/md.cjs'
 ];
 
 async function build() {

commit ba76961dbbc15cd12204eb43052bf68cafd9c235
Author: jintrick <jintrick@gmail.com>
Date:   Tue Feb 17 22:48:40 2026 +0900

    feat: complete separation of validation and auto-open UI (v1.5.2)

diff --git a/tools/build.cjs b/tools/build.cjs
index 7c72dd1..44a3a83 100644
--- a/tools/build.cjs
+++ b/tools/build.cjs
@@ -4,7 +4,8 @@ const fs = require('fs');
 
 const hooks = [
   'hooks/scripts/linter_hook.cjs',
-  'hooks/scripts/expert_docs_hook.cjs'
+  'hooks/scripts/expert_docs_hook.cjs',
+  'hooks/scripts/open_file_hook.cjs'
 ];
 
 const linters = [
@@ -12,8 +13,7 @@ const linters = [
   'hooks/scripts/linters/cjs.cjs',
   'hooks/scripts/linters/mjs.cjs',
   'hooks/scripts/linters/ts.cjs',
-  'hooks/scripts/linters/json.cjs',
-  'hooks/scripts/linters/md.cjs'
+  'hooks/scripts/linters/json.cjs'
 ];
 
 async function build() {

commit e2568caf4f65574d8a37453789f48706b7e2cb7c
Author: = <=>
Date:   Mon Feb 16 16:08:48 2026 +0900

    start: v1.2.0 implementation plan and skeleton

diff --git a/tools/build.cjs b/tools/build.cjs
index dedc51a..7c72dd1 100644
--- a/tools/build.cjs
+++ b/tools/build.cjs
@@ -3,7 +3,8 @@ const path = require('path');
 const fs = require('fs');
 
 const hooks = [
-  'hooks/scripts/linter_hook.cjs'
+  'hooks/scripts/linter_hook.cjs',
+  'hooks/scripts/expert_docs_hook.cjs'
 ];
 
 const linters = [

commit 47f88fe19125843dce1ecd383ab2b1438abc10a2
Author: = <=>
Date:   Mon Feb 16 13:14:22 2026 +0900

    feat: implement automated versioning and release management in v1.1.1

diff --git a/tools/build.cjs b/tools/build.cjs
new file mode 100644
index 0000000..dedc51a
--- /dev/null
+++ b/tools/build.cjs
@@ -0,0 +1,46 @@
+const esbuild = require('esbuild');
+const path = require('path');
+const fs = require('fs');
+
+const hooks = [
+  'hooks/scripts/linter_hook.cjs'
+];
+
+const linters = [
+  'hooks/scripts/linters/js.cjs',
+  'hooks/scripts/linters/cjs.cjs',
+  'hooks/scripts/linters/mjs.cjs',
+  'hooks/scripts/linters/ts.cjs',
+  'hooks/scripts/linters/json.cjs',
+  'hooks/scripts/linters/md.cjs'
+];
+
+async function build() {
+  // hooks をビルド
+  for (const hook of hooks) {
+    await esbuild.build({
+      entryPoints: [hook],
+      bundle: true,
+      platform: 'node',
+      outfile: path.join('dist', hook.replace('hooks/scripts/', 'hooks/')),
+      external: ['./linters/*'], // リンターの動的requireは外部参照として残す
+    });
+  }
+
+  // linters をビルド
+  for (const linter of linters) {
+    await esbuild.build({
+      entryPoints: [linter],
+      bundle: true,
+      platform: 'node',
+      outfile: path.join('dist', linter.replace('hooks/scripts/', 'hooks/')),
+    });
+  }
+
+  console.log('Build completed successfully!');
+}
+
+build().catch(err => {
+  console.error(err);
+  process.exit(1);
+});
